{
  "$comment": "CAT-F floor-to-real transition, Primary app root. The installed floor version has no service manifest; the real candidate does. Transaction selection must read both manifests plus the stable package id and must not assume an installed package without a service is a non-service package.",
  "scenario": "floor-to-real",
  "install_target": "primary",
  "installed": {
    "id": "org.umrk.syncthing",
    "version": "0.0.0-floor",
    "manifest": {
      "id": "org.umrk.syncthing",
      "name": "Syncthing (unavailable on this Leaf version)"
    },
    "note": "No 'service' object at all -- this is CAT-F's inert floor pak, not a disabled service."
  },
  "candidate": {
    "id": "org.umrk.syncthing",
    "version": "1.0.0",
    "manifest": {
      "id": "org.umrk.syncthing",
      "service": {
        "schema": 1,
        "id": "org.umrk.syncthing",
        "run": {"path": "bin/leaf-syncthing", "args": ["service", "run"]},
        "restart": "on-failure",
        "default_enabled": false,
        "stop_grace_ms": 10000,
        "lifecycle": {"game": "notify", "stop_on_storage_change": true, "stop_on_suspend": true}
      },
      "state": {"root": "Syncthing", "revoke_on_uninstall": ["leaf/trusted-clients.json"], "retained_roots": ["Syncthing"]}
    }
  },
  "expected_outcome": {
    "allowed": true,
    "action": "Ordinary P1 promote: move floor tree aside, promote real tree, commit install record. TXN-1 quiescence proof is trivially satisfied because the floor pak never had a service.",
    "post_state": "service.id org.umrk.syncthing now discoverable and startable; default_enabled remains false so the user must explicitly enable it."
  }
}
